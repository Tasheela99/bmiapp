import 'package:flutter/cupertino.dart';

class AppFunctions{

  void clearTextFields(TextEditingController controller) {
    controller.clear();
    controller.clear();
    controller.clear();
    controller.clear();
    controller.clear();
  }
}